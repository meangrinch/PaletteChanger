# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging (Keep basic ones, remove most)
build/
dist/
*.egg-info/
*.egg

# Environments (Keep the common ones)
.env
.venv
env/
venv/

# VS Code (Keep basic ones)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv (likely not needed for a GUI app, but harmless to keep)
.python-version

# mypy (if you start using it)
.mypy_cache/

# Project-Specific (VERY IMPORTANT)
# Add any files/folders your application GENERATES that you don't want in Git.
# output/   # Example: If you save converted images to an 'output' folder.
# *.log    # Any log files your app might create.

# OS-Specific files (good to have)
.DS_Store  # macOS
Thumbs.db  # Windows